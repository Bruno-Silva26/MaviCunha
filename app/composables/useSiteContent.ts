import * as conteudo from '~/data/site'

/**
 * Acesso único e auto-importado ao conteúdo editorial.
 * Use `const { candidata, links } = useSiteContent()` dentro dos componentes.
 */
export const useSiteContent = () => conteudo
